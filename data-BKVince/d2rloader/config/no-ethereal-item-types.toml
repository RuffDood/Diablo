# Item type codes come from the Code column of itemtypes.txt.
# Parent types use D2R's native inheritance: "armo" covers boot, glov, tors, and other children.

[ethereal_exclusions]
enabled = true

# To target multiple types, use a single TOML array:
# item_types = ["boot", "glov", "tors"]
# Quote every code and separate each entry with a comma.
item_types = []
